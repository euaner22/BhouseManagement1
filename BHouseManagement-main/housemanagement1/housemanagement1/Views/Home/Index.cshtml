<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bhousemanagement System</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-image: url('../../Contents/img/IMG_0452-scaled.jpg');
            background-size: cover;
            background-position: center;
            height: 100vh;
        }

        .container {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
            text-align: center;
            color: white;
        }

        .text-overlay {
            background-color: rgba(0, 0, 0, 0.5); /* Transparent black background */
            padding: 20px; /* Add padding to create space around the text */
            border-radius: 10px; /* Rounded corners */
        }

        /* Button styling */
        .btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            margin: 5px;
        }

            .btn:hover {
                background-color: #0056b3;
            }

        /* Welcome and BHOUSEMANAGEMENT styling */
        .welcome-text, h2, p {
            color: white;
 
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="text-overlay">
            <h1 class="welcome-text">Welcome to</h1>
            <h2 style="font-family: 'Arial Black', sans-serif;">BHOUSEMANAGEMENT</h2>
            <p style="font-family: 'Arial', sans-serif;">Your reliable solution for house management</p>
            <a href="@Url.Action("Index", "Home")" class="btn">Home</a>
            <a href="@Url.Action("Create", "Home")" class="btn">Register</a>
            <a href="@Url.Action("Login", "Home")" class="btn">Login</a>
        </div>
    </div>
</body>
</html>
